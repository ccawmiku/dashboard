# Dashboard 开发约束

## 先读

README.md 和 docs/01-product.md、02-architecture.md、03-contracts.md。连接器任务另读 04；组件任务另读 05；时钟任务另读 06；部署任务另读 08。文档中的路线规划不代表已经实现。

## 必须保持

- TypeScript 严格模式；前后端契约用运行时 schema 校验。
- 连接器、数据核心、组件、看板外壳职责独立。
- 一个组件类型只有一个固定尺寸；新尺寸创建新类型。
- 组件不 fetch 外部来源、不保存凭证、不导入后端内部实现。
- 数据点 ID 稳定且不复用；失败不伪造成 0 或 good。
- 单调时间与 UTC epoch 分工明确；不得拿客户端 Date.now 冒充 NTP。
- 持久化默认值幂等初始化；重启后不能恢复“已同步”的运行时信任。
- 不把 secrets、数据库、.env、构建结果提交到仓库。
- 新功能同时更新文档、预览和适当测试；未真实验证必须明确记录。

## 验证与交付

运行 pnpm check、相关浏览器测试、依赖审计和秘密扫描。检查实际结果后才能报告通过。

只在 main 开发。每个完整提交对应一个唯一 annotated vMAJOR.MINOR.PATCH；首个 v1.0.0。推送提交和标签，不自动创建 Release。不强推、不改写历史、不删除远程资源。

禁止本地 Docker/Podman 构建或测试；容器仅在 GitHub Actions 构建测试。基础镜像使用明确版本，默认仅 linux/amd64。GHCR 发布版本标签，不发布 latest。

使用用户已授权的范围自主完成工作；新增破坏性协议、数据迁移破坏、敏感暴露或破坏性操作需另行确认。
